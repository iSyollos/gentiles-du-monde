name: GitHub Pages manual workflow

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skip automatic deployment
        run: echo "No automatic deployment configured. Use GitHub Pages Settings to publish from main root."
